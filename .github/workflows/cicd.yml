name: KBOT-CI-CD

on: push

jobs:
    ci:
        name: CI
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v3
              with:
                go-version: '1.21'

            - run: go version

            - name: Run test
              run:  make test  